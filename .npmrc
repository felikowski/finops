# Flat node_modules (npm-like) for maximum tooling compatibility — Angular CLI
# in particular expects a hoisted layout. Revisit once everything is verified
# under pnpm's default isolated linker.
node-linker=hoisted
