# Where the app reads its secrets from at startup:
#   local     → read this .env file (default)
#   infisical → fetch from the Infisical instance and inject into the environment
SECRETS_SOURCE=local

# In local mode these are read from here (or fall back to app.module defaults).
# When SECRETS_SOURCE=infisical, ALL of the DB_* values below are overwritten
# from Infisical: connection info (host/port/database/schema) from /postgres,
# and DB_USER/DB_PASSWORD from /postgres/finops/finops_owner. The values below
# are just the local defaults.
DB_HOST=localhost
DB_PORT=5432
DB_USER=finops_owner
DB_PASSWORD=postgres
DB_NAME=finops
DB_SCHEMA=finops

# Global fallback AWS identity, used only for billing accounts that don't set
# their own `credentialRef` (billing_accounts registry, issue #16). Region is
# NOT read from here anymore — each billing account carries its own
# sourceConfig.region, resolved at pull time.
# When SECRETS_SOURCE=infisical, both are overwritten from Infisical
# (keys "access_key_id"/"secret_access_key" at /aws/finops/billing-s3-reader).
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Auth0 (issue #20) — human login for the UI/API. These are plain config, not
# secrets (they end up in the JWT `iss`/`aud` claims and the SPA's login
# redirect anyway, so there's nothing to protect by hiding them).
#   AUTH0_DOMAIN    — your tenant domain, e.g. finops.eu.auth0.com (no https://)
#   AUTH0_CLIENT_ID — the SPA application's Client ID (relayed to the frontend
#                     via GET /config.json, never used by the backend itself)
#   AUTH0_AUDIENCE  — the API identifier registered in Auth0, used both to
#                     request an access token (frontend) and to validate one
#                     (backend, as the JWT `aud` claim)
# When SECRETS_SOURCE=infisical, all three are overwritten from Infisical
# (keys "domain"/"client_id"/"audience" at /auth0). The values below are just
# the local defaults.
AUTH0_DOMAIN=
AUTH0_CLIENT_ID=
AUTH0_AUDIENCE=

# DuckLake catalog (issue #49) — separate from DB_*/AWS_* above on purpose, so
# DuckLake-specific config doesn't mix with the TypeORM/billing-ingestion
# config. DUCKLAKE_DB_HOST/PORT point at the SAME Postgres instance as DB_HOST/
# DB_PORT above (see docs/runbooks/ducklake-catalog-provisioning.md) — only the
# database/role/bucket differ.
# When SECRETS_SOURCE=infisical, all of these are overwritten from Infisical:
# host/port from /postgres, database from /postgres/ducklake, user/password
# from /postgres/ducklake/ducklake_owner, and the AWS_* ones from
# /aws/finops/ducklake. Empty here since there's no local DuckLake to point at
# by default — fill these in only if you're working on issue #49 directly.
DUCKLAKE_DB_HOST=
DUCKLAKE_DB_PORT=
DUCKLAKE_DB_NAME=
DUCKLAKE_DB_USER=
DUCKLAKE_DB_PASSWORD=
DUCKLAKE_AWS_ACCESS_KEY_ID=
DUCKLAKE_AWS_SECRET_ACCESS_KEY=
DUCKLAKE_S3_BUCKET=
DUCKLAKE_S3_REGION=

# Infisical — only required when SECRETS_SOURCE=infisical.
# INFISICAL_CLIENT_ID / INFISICAL_CLIENT_SECRET are the machine-identity
# credentials ("secret zero") and must be supplied via the environment.
# INFISICAL_ENVIRONMENT is the environment *slug* in Infisical (the default
# Development environment's slug is "dev", not "development").
# The paths/keys the app fetches are declared in secrets.bootstrap.ts; see
# docs/infisical.md for the full required vault structure.
INFISICAL_SITE_URL=https://your-infisical-instance
INFISICAL_PROJECT_ID=
INFISICAL_ENVIRONMENT=dev
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
