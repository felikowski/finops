{
  "name": "finops-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "db": "docker compose up -d",
    "start": "concurrently \"npm run start:dev --prefix backend\" \"npm run start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}